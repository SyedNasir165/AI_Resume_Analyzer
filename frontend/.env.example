# Base URL of the backend API
VITE_API_BASE_URL=http://localhost:8000

# Supabase — Project Settings -> API. The anon/publishable key is safe to expose in
# frontend code; Row Level Security on every table is what actually enforces access control.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
